<UserControl x:Class="Paket.Ui.Csharp.BrowseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Paket.Ui.Csharp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance local:BrowseViewModel}"
             d:DesignHeight="600"
             d:DesignWidth="600"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <TextBox Text="{Binding SearchText}" />
                <Button Command="{Binding RefreshCommand}" Content="Refresh" />
                <CheckBox Content="Include prerelease" IsChecked="{Binding IsIncludingPreRelease}" />
            </StackPanel>

            <Grid Grid.Column="1" HorizontalAlignment="Right">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Text="Manage Packages for Solution" />

                <StackPanel Grid.Row="1"
                            HorizontalAlignment="Left"
                            Orientation="Horizontal">
                    <TextBlock Text="Package source: " />
                    <ComboBox ItemsSource="{Binding PackageSources}" SelectedItem="{Binding SelectedPackageSource}" />
                    <CheckBox Content="Include prerelease" IsChecked="{Binding IsIncludingPreRelease}" />
                </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="3" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <local:PackageListView Packages="{Binding FeedPackages}" SelectedPackage="{Binding SelectedDependency}" />

            <GridSplitter Grid.Column="1" />

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="3" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <GroupBox Header="Projecs">
                    <TextBlock Text="PLACEHOLDER" />
                </GroupBox>

                <GridSplitter Grid.Row="1" />

                <GroupBox Grid.Row="2" Header="Info">
                    <TextBlock Text="PLACEHOLDER" />
                </GroupBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
